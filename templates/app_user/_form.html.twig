{{ form_start(form, {'attr': {'class': 'needs-validation', 'novalidate': ''}}) }}
    <h1>{{ heading_label|default('Registration') }}</h1>
{{ form_widget(form) }}
    <button class="form-button form-button__confirm">{{ button_label|default('Register') }}</button>
{{ form_end(form) }}
