{% extends 'base.html.twig' %}

{% block title %}Reservations for {{ room.codeName }}{% endblock %}

{% block body %}
    {% include 'components/_header.html.twig' %}
    <div class="content-wrapper">
        <h1>Reservations for {{ room.codeName }}</h1>

        <h2>My reservations</h2>
        {{ include('reservation/_list.html.twig', { reservations: reservations }) }}
        <h2></h2>

        {% if is_granted('room_create') %}
            <a href="{{ path('app_room_reservation_new', { roomId: room.id }) }}">Create new reservation</a>
        {% endif %}
    </div>
    {% include 'components/_footer.html.twig' %}
{% endblock %}
