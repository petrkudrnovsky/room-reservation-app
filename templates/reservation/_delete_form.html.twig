<form method="post" action="{{ path('app_room_reservation_delete', {'id': reservation.id, 'roomId': reservation.room.id}) }}" onsubmit="return confirm('Are you sure you want to delete this reservation?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation.id) }}">
    <button class="form-button form-button__delete">Delete</button>
</form>
