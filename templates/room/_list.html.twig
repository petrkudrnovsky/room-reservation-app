<ul>
    {% for room in rooms %}
        <li>
            <a href="{{ path('app_room_show', {'id': room.id}) }}">{{ room.codeName }}</a>
        </li>
    {% else %}
        <li>No rooms found</li>
    {% endfor %}
</ul>